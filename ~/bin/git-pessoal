#!/bin/bash

echo "👤 Configurando Git para PESSOAL..."

# Configuração global para conta pessoal
git config --global user.name "José Araújo"
git config --global user.email "dansa.rectech@gmail.com"

# Remover configurações de URL da empresa (usar GitHub padrão)
git config --global --unset url."git@github.com:mercadolibre/".insteadOf
git config --global --unset url."git@github.com-emu:melisource/".insteadOf

echo "✅ Git configurado para PESSOAL!"
echo "📧 Email: dansa.rectech@gmail.com"
echo "🔑 SSH: Para projetos pessoais, use:"
echo "   git clone git@github.com-personal:seu-usuario/repo.git"

# Mostrar configuração atual
echo ""
echo "📋 Configuração atual:"
git config --global --get user.name
git config --global --get user.email

echo ""
echo "💡 DICA: Para clonar repositórios pessoais, use:"
echo "   git clone git@github.com-personal:SEU_USUARIO/REPO.git"
